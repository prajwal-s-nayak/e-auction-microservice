package com.cts.microservices.buyerservice.kafka;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Service;

import com.cts.microservices.buyerservice.model.PlaceBidRequest;

@Service
public class AuctionConsumer {
	
	public static final Logger LOGGER = LoggerFactory.getLogger(AuctionConsumer.class);

//	@KafkaListener(topics = "auction_topic", groupId = "cosumergroup1")
//	public void consume(PlaceBidRequest placeBidRequest) {
//		LOGGER.info(String.format("Auction data", placeBidRequest.toString()));
//	}
	
	@KafkaListener(topics = "auction", groupId = "cosumergroup1")
	public void consume(String placeBidRequest) {
		LOGGER.info(String.format("Auction data -> %s", placeBidRequest));
	}
}
