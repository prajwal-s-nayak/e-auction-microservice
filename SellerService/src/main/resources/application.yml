server:
  port: 8082
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SellerDB
    password: pass@word1
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect    show-sql: true 

  application:
    name: SELLER-SERVICE